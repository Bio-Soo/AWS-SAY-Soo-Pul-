import React from 'react';
import RareLinkApp from './LoginWorklist.jsx';

/**
 * App root.
 * 디자인 시스템 showcase 는 설정 화면(설정 → 디자인 시스템)에서 진입한다.
 * (과거: 우측 하단 dev 스위처 → 제거됨)
 */
export default function App() {
  return <RareLinkApp />;
}

# Dev mode 기본값 — Mock(true) ↔ Real SMART(false) 토글 (CLAUDE.md §9)
# 개인 SMART 검증 시 frontend/.env.development.local 에서 false로 override.
VITE_USE_MOCK=true

# 세션 TTL (분). 기본 60 = 1시간.
# 운영자가 보안 정책에 따라 변경 가능 (15~480 권장).
VITE_SESSION_TIMEOUT_MIN=60
